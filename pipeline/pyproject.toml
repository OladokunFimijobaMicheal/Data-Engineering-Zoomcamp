[project]
name = "pipeline"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.5",
    "pyarrow>=25.0.0",
]

[dependency-groups]
dev = [
    "pgcli>=4.5.0",
]
